- name: install mysql package playbook
  become: yes
  hosts: node
  ignore_errors: true
  tasks:
    - name: install mysql task
      ansible.buitin.package:
        name: mysql
        state: present